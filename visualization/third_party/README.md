# Third Party — intentionally empty (minimal supply chain)

No binary third-party addons are vendored. See `DEPENDENCIES.md` and `THIRD_PARTY.md` for evaluated candidates and rejection reasons.

If an addon is approved, place it under `visualization/addons/<name>/` with `SOURCE.md` + `LICENSE`.
