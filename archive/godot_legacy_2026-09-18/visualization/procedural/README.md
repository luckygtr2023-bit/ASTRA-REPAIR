# Procedural

- `noise_generator.gd` — FastNoiseLite wrapper (tilable, seeded)
- `planet_albedo.py` — offline 512 albedo bake (CC0, no Pillow needed)
- Future: `galaxy_generator.py` (log spiral arms), `crater_distribution.py`

All procedural outputs are deterministic given seed (replay-safe) and CC0.
